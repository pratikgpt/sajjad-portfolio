Drop project screenshots here, matching the `image` paths in src/lib/site.ts:

  meditranslate.png   — MediTranslate
  novaml.png          — NovaML
  troopod.png         — Troopod
  claim-pipeline.png  — Claim Processing Pipeline

Recommended: ~16:10 aspect (e.g. 1280x800). Until a file exists, the card
shows a subtle placeholder block instead of a broken image.
